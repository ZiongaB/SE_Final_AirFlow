import { Packinglist } from "./PackingList";

/**
 * Model for the Packing data in order to link the item and boolean together.
 * @author Andrew Gordon
 */
export interface Packing {
    item:String,
    checked:Boolean
}